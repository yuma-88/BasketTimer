<% content_for :title, "トップ" %>

<div data-controller="audio touch-protection" class="flex flex-col items-center justify-center min-h-[100svh] text-white text-center bg-black fixed top-0 left-0 right-0 bottom-0 z-50">
  <h1 class="text-[clamp(3rem,16vw,15rem)] font-bold mb-6">B-Timer</h1>
  
  <%= link_to menus_path, class: "bg-zinc-900 p-6 rounded-lg shadow-lg hover:bg-zinc-700 block text-center text-white border border-orange-500", data: { action: "click->audio#navigateWithSound" } do %>
    <span class="text-xl font-semibold">START</span>
  <% end %>
</div>
