<% content_for :title, "トップ" %>

<div data-controller="audio" class="flex flex-col items-center justify-center min-h-screen text-white text-center bg-black">
  <h1 class="text-7xl font-bold mb-6">BasketTimer</h1>
  
  <button onclick="window.location='<%= menus_path %>'" data-action="click->audio#playSwichSound" class="bg-zinc-900 p-6 rounded-lg shadow-lg hover:bg-zinc-700 block text-center text-white border border-orange-500">
    <span class="text-xl font-semibold">STRAT</span>
  </button>
</div>
